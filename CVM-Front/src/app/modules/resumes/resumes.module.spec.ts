import { ResumesModule } from './resumes.module';

describe('ResumesModule', () => {
  const module: ResumesModule = new ResumesModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
