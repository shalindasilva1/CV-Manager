import { ResumesAddComponent } from './resumes-add.component';

describe('ResumesAddComponent', () => {
  let component: ResumesAddComponent;

  beforeEach(() => {
    component = new ResumesAddComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
