import { MatiTableComponent } from './mati-table.component';

describe('MatiTableComponent', () => {
  let component: MatiTableComponent;

  beforeEach(() => {
    component = new MatiTableComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
